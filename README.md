# prism

A zero-boilerplate, cross-platform graphics foundation for building a GUI ecosystem.  
Prism wraps **wgpu** + **winit** into a tiny, painless API — just open a window and draw.

*The lowest layer of a larger UI project. More layers coming.*
